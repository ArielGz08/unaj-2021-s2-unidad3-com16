# Vamos a ver el significado de abstracción con un ejemplo

def potencia(a,b):
    resultado = pow(a,b) #a**b
    return resultado

# La función potencia la podría programar usan ** o usando pow o cualquier otra forma
# Esa es la abstracción


print(potencia(2, 3))     

